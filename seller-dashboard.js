let orders=[];
document.addEventListener('DOMContentLoaded',async()=>{await kcReady;if(!KC.currentUserId)return;mountIcons();renderBottomNav('profile');try{orders=await KC_API.get('/api/v1/seller/orders');render();}catch(e){document.getElementById('sellerOrders').innerHTML='<div class="empty-state"><div class="empty-title">'+escapeHtml(e.message)+'</div></div>'}});
function render(){const pending=orders.filter(o=>o.status==='pending').length, revenue=orders.filter(o=>o.status==='delivered').reduce((a,o)=>a+Number(o.total||0),0);document.getElementById('stats').innerHTML=`<div class="card" style="padding:14px"><div style="font-size:12px;color:var(--ink-muted)">মোট অর্ডার</div><div style="font-size:25px;font-weight:800">${orders.length}</div></div><div class="card" style="padding:14px"><div style="font-size:12px;color:var(--ink-muted)">অপেক্ষমাণ</div><div style="font-size:25px;font-weight:800">${pending}</div></div><div class="card" style="padding:14px"><div style="font-size:12px;color:var(--ink-muted)">ডেলিভারি সম্পন্ন</div><div style="font-size:25px;font-weight:800">${orders.filter(o=>o.status==='delivered').length}</div></div><div class="card" style="padding:14px"><div style="font-size:12px;color:var(--ink-muted)">সম্পন্ন বিক্রি</div><div style="font-size:25px;font-weight:800">৳ ${revenue.toLocaleString('bn-BD')}</div></div>`;document.getElementById('sellerOrders').innerHTML=orders.slice(0,20).map(o=>`<div class="card" style="padding:14px;margin-bottom:10px"><div style="display:flex;justify-content:space-between"><b>#${escapeHtml(o.id)}</b><span class="chip">${escapeHtml(statusLabel(o.status))}</span></div><div style="font-size:13px;margin-top:7px">৳ ${Number(o.total).toLocaleString('bn-BD')}</div><div style="font-size:12px;color:var(--ink-muted);margin-top:4px">${(o.items||[]).map(i=>escapeHtml(i.name)+' × '+i.quantity).join(' · ')}</div><div style="display:flex;gap:7px;flex-wrap:wrap;margin-top:10px">${actions(o)}</div></div>`).join('')||'<div class="empty-state"><div class="empty-title">এখনও কোনো অর্ডার নেই</div></div>'}
function statusLabel(s){return {pending:'অপেক্ষমাণ',confirmed:'নিশ্চিত',processing:'প্রস্তুত হচ্ছে',shipped:'পাঠানো হয়েছে',delivered:'ডেলিভারি সম্পন্ন',cancelled:'বাতিল'}[s]||s}
function actions(o){const a=[];if(o.status==='pending')a.push(btn(o,'confirmed','নিশ্চিত করুন'));if(o.status==='confirmed')a.push(btn(o,'processing','প্রসেসিং'));if(o.status==='processing')a.push(btn(o,'shipped','শিপ করুন'));if(o.status==='shipped')a.push(btn(o,'delivered','ডেলিভারি সম্পন্ন'));return a.join('')}
function btn(o,s,label){return `<button class="btn btn-outline" data-order="${escapeHtml(o.id)}" data-status="${s}">${label}</button>`}
document.addEventListener('click',async e=>{const b=e.target.closest('[data-order]');if(!b)return;try{await KC_API.patch('/api/v1/orders/'+encodeURIComponent(b.dataset.order)+'/status?status='+encodeURIComponent(b.dataset.status));orders=await KC_API.get('/api/v1/seller/orders');render()}catch(err){toast(err.message)}});
