document.addEventListener("DOMContentLoaded",async()=>{await kcReady;if(!KC.currentUserId)return;mountIcons();renderBottomNav("");const box=document.getElementById("searchBox");box.oninput=()=>runSearch(box.value.trim());runSearch("")});
async function runSearch(q){const wrap=document.getElementById("resultsWrap");if(!q){wrap.innerHTML=`<div class="empty-state"><div class="empty-emoji">🔍</div><div class="empty-title">মানুষ, পণ্য, #ট্যাগ বা পোস্ট খুঁজুন</div></div>`;return}try{const [people,data]=await Promise.all([KC_API.get('/api/v1/users?search='+encodeURIComponent(q)),KC_API.get('/api/v1/marketplace/search?q='+encodeURIComponent(q))]);const products=data.products||[];const ads=data.ads||[];const posts=(KC.posts||[]).filter(p=>(p.text||'').toLowerCase().includes(q.toLowerCase()));let html='';if(people.length)html+=`<div class="section-title">মানুষ</div>`+people.map(u=>`<a href="profile.html?id=${u.id}" class="chat-row"><img class="avatar avatar-42" src="${u.avatar||''}"><div><div class="chat-name">${escapeHtml(u.name)}</div><div class="chat-preview">${escapeHtml(u.location||'')}</div></div></a>`).join('');if(ads.length)html+=`<div class="section-title" style="margin-top:14px">আপনার এলাকার Sponsored Ads</div>`+ads.map(a=>`<a class="card" href="${a.landing_url||('product.html?id='+a.product_id)}" style="display:block;padding:12px;margin-bottom:9px"><div style="font-size:11px;font-weight:800">📣 Sponsored</div><div style="font-weight:800;margin-top:3px">${escapeHtml(a.headline)}</div><div class="muted">${escapeHtml(a.page_name||'')}</div><div style="font-size:12px;margin-top:5px">${escapeHtml((a.tags||[]).map(t=>'#'+t).join(' '))}</div></a>`).join('');if(products.length)html+=`<div class="section-title" style="margin-top:14px">পণ্য</div><div class="product-grid" style="padding:0">`+products.map(p=>`<a class="product-card" href="product.html?id=${p.id}"><div class="product-img-wrap"><img src="${p.images?.[0]||''}"></div><div class="product-body"><div class="product-name">${escapeHtml(p.name)}</div><div class="product-price">৳ ${Number(p.price).toLocaleString('bn-BD')} / ${escapeHtml(p.unit||'')}</div><div style="font-size:11px;margin-top:3px">${escapeHtml((p.tags||[]).map(t=>'#'+t).join(' '))}</div></div></a>`).join('')+'</div>';if(posts.length)html+=`<div class="section-title" style="margin-top:14px">পোস্ট</div>`+posts.map(p=>`<a href="post.html?id=${p.id}" class="card" style="display:block;padding:11px 13px;margin-bottom:9px">${escapeHtml(p.text.slice(0,120))}</a>`).join('');wrap.innerHTML=html||`<div class="empty-state"><div class="empty-emoji">😕</div><div class="empty-title">কিছু পাওয়া যায়নি</div></div>`}catch(e){wrap.innerHTML=`<div class="empty-state">${escapeHtml(e.message)}</div>`}}
